# Server profile: Headless server configuration
# Development tools without GUI applications
{ config, lib, ... }:

{
  # Server-specific defaults are set in profiles/default.nix
  # This file can contain additional server-specific configuration
}
