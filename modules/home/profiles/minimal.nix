# Minimal profile: Core tools only
# This profile provides essential CLI tools for any environment
{ config, lib, ... }:

{
  # The minimal profile is the base - it doesn't enable any extra features
  # Core modules are always loaded regardless of profile
}
